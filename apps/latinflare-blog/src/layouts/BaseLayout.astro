---
interface Props {
  title: string;
  description?: string;
  image?: string;
  fullWidth?: boolean;
}

const {
  title,
  description = 'LatinFlare Blog — dating tips, success stories, and guides for Latin singles.',
  image,
  fullWidth = false,
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const pageTitle = `${title} — LatinFlare Blog`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}/favicon.svg`} />
    <link rel="icon" type="image/png" sizes="32x32" href={`${import.meta.env.BASE_URL}/favicon-32x32.png`} />
    <link rel="icon" type="image/png" sizes="16x16" href={`${import.meta.env.BASE_URL}/favicon-16x16.png`} />
    <link rel="apple-touch-icon" sizes="180x180" href={`${import.meta.env.BASE_URL}/apple-touch-icon.png`} />
    <meta name="theme-color" content="#4F378B" />
    <link rel="sitemap" href={`${import.meta.env.BASE_URL}/sitemap-index.xml`} />
    <link rel="alternate" type="application/rss+xml" title="LatinFlare Blog" href={`${import.meta.env.BASE_URL}/rss.xml`} />
    <meta name="generator" content={Astro.generator} />

    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    {image && <meta property="og:image" content={image} />}

    <!-- Twitter -->
    <meta name="twitter:card" content={image ? 'summary_large_image' : 'summary'} />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={description} />
    {image && <meta name="twitter:image" content={image} />}

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Urbanist:wght@400;600;700;800;900&family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet" />

    <!-- Prevent flash of wrong theme by applying preference before paint -->
    <script is:inline>
      (function () {
        try {
          var stored = localStorage.getItem('theme-preference') || 'system';
          var dark = stored === 'dark' ||
            (stored === 'system' && window.matchMedia('(prefers-color-scheme: dark)').matches);
          document.documentElement.setAttribute('data-theme', dark ? 'dark' : 'light');
        } catch (_) {
          var dark = window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches;
          document.documentElement.setAttribute('data-theme', dark ? 'dark' : 'light');
        }
      })();
    </script>

    <slot name="head" />

    <style>
      *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

      /* Light theme (default) */
      :root {
        --color-primary: #4F378B;
        --color-on-primary: #ffffff;
        --color-text: #1a1a2e;
        --color-muted: #6b7280;
        --color-bg: #fafafa;
        --color-bg-surface: #ffffff;
        --color-border: #e5e7eb;
        --color-code-bg: #f3e8ff;
        --color-tag-bg: #e9d5ff;
        --color-tag-text: #6b21a8;
        --color-tag-hover: #d8b4fe;
        --font-display: 'Urbanist', system-ui, sans-serif;
        --font-sans: 'Plus Jakarta Sans', system-ui, -apple-system, sans-serif;
        --max-width: 1100px;
      }

      /* Dark theme — from Flutter LatinFlare dark config */
      [data-theme='dark'] {
        --color-primary: #b39ddb;
        --color-on-primary: #1a0a3d;
        --color-text: #f1f5f9;
        --color-muted: #94a3b8;
        --color-bg: #1c1a20;
        --color-bg-surface: #2a2733;
        --color-border: #3d3a47;
        --color-code-bg: #2a1f3d;
        --color-tag-bg: #4a1d96;
        --color-tag-text: #c4b5fd;
        --color-tag-hover: #5b21b6;
      }

      body {
        font-family: var(--font-sans);
        background: var(--color-bg);
        color: var(--color-text);
        line-height: 1.6;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        transition: background-color 0.2s ease, color 0.2s ease;
      }
      main { flex: 1; width: 100%; max-width: var(--max-width); margin: 0 auto; padding: 2rem 1rem; }
      main.full-width { max-width: none; padding: 0; margin: 0; }
      a { color: var(--color-primary); text-decoration: none; }
      a:hover { text-decoration: underline; }
      .sr-only {
        position: absolute;
        width: 1px;
        height: 1px;
        padding: 0;
        margin: -1px;
        overflow: hidden;
        clip: rect(0,0,0,0);
        white-space: nowrap;
        border-width: 0;
      }
    </style>
  </head>
  <body>
    <slot name="header" />
    <main class={fullWidth ? 'full-width' : undefined}>
      <slot />
    </main>
    <slot name="footer" />
  </body>
</html>
