{
  "name": "@ruvikgroup/cms-config",
  "version": "0.0.1",
  "private": true,
  "description": "DecapCMS configuration (placeholder)",
  "main": "index.js",
  "scripts": {
    "build": "echo 'cms-config: no build step yet'",
    "lint": "echo 'cms-config: no lint step yet'"
  }
}
